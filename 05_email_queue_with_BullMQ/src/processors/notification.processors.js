export async function processNotification(job) {
    switch (job.name) {
        case 'streak-alert':
            
            break;
        case 'potd-alert':
            
            break;
    
        default:
            break;
    }
}